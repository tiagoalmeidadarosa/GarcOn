<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 x:Class="GarcOn.Pages.RequestAccountPopupPage"
                 BackgroundColor="{StaticResource primaryColor}">
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation DurationIn="400"
                                   DurationOut="300"
                                   EasingIn="SinOut"
                                   EasingOut="SinIn"
                                   HasBackgroundAnimation="True"
                                   PositionIn="Center"
                                   PositionOut="Center"
                                   ScaleIn="1.2"
                                   ScaleOut="0.8" />
    </pages:PopupPage.Animation>

    <StackLayout Margin="12"
                 Padding="24"
                 BackgroundColor="White"
                 HorizontalOptions="Center"
                 VerticalOptions="Center">
        <Grid x:Name="gridItens" Margin="0, 0, 0, 10" ColumnSpacing="20" />

        <Label Text="* Caso sinta-se à vontade deixe uma sugestão:" TextColor="Black" FontSize="Small" />
        <Editor HeightRequest="100" FontSize="Small" />

        <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
            <Button BackgroundColor="White"
                    Text="Cancelar"
                    TextColor="Black"
                    Clicked="CancelButton_Clicked"/>
            <Button BackgroundColor="Red"
                    Text="Confirmar"
                    TextColor="White"
                    Clicked="ConfirmButton_Clicked"/>
        </StackLayout>
    </StackLayout>
</pages:PopupPage>